// Hàm mở link bài tập trong tab mới
function openInNewTab(link) {
    window.open(link, '_blank').focus();
}

// Hàm mở link liên lạc trong tab mới
function openLink(url) {
    window.open(url, '_blank').focus();
}
